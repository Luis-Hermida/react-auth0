{
  "name": "auth0",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd view && npm install && npm run build",
    "install-local": "cd view && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "auth0-js": "^9.12.2",
    "auth0-lock": "^11.22.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-jwt": "^6.0.0",
    "express-jwt-authz": "^2.3.1",
    "jwks-rsa": "^1.7.0"
  }
}
